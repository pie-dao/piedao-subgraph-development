type Stat @entity {
  id: ID!,
  locksCounter: BigInt!
}

type Account @entity {
  id: ID!
  locks: [Lock!]! @derivedFrom(field: "account")
}

type Lock @entity {
  id: ID!
  lockId: BigInt!
  lockDuration: BigInt!
  lockedAt: BigInt!
  amount: BigInt!
  account: Account!
  withdrawn: Boolean!
}
